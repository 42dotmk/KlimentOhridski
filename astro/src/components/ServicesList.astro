---
import { getAbsoluteUrl } from "../lib/strapi";
import type { Service } from "../types";
import ServiceCard from "./ServiceCard.astro";

const { services } = Astro.props;
---

<div class="flex flex-col justify-center items-center px-4 gap-16">
  <h1 class="text-3xl sm:text-5xl font-bold text-center pb-8 lg:pb-0">
    Услуги
  </h1>
  <div class="flex flex-wrap gap-8 max-w-[1160px] justify-center items-center">
    {services.map((service: Service) => (
        <ServiceCard imageUrl={getAbsoluteUrl(service.image?.url)} name={service.name} />
    ))}
</div>
</div>
